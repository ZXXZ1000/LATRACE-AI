# Phase 5 Ground Truth 样本格式

这里存放 Phase 5 评估所需的最小 JSONL 样本（模板/小样本）。

建议：
- 真实评估请使用内部 benchmark 或标注集扩展此目录。
- 每个 JSONL 文件每行一个样本，必须包含 `query_id`。

文件清单（模板）：
- `topic_timeline.jsonl`
- `entity_profile.jsonl`
- `quotes.jsonl`
- `relations.jsonl`
- `time_since.jsonl`

